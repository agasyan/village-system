version: '3.7'

services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    volumes:
      - './client:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - "3000:3000"
    container_name: client-react-ui
    stdin_open: true